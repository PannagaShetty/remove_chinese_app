name: delete_china_apps
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  device_apps: ^1.0.9
  uninstall_apps: ^0.0.4
  expandable: ^4.1.4
  url_launcher: ^5.4.11


dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:

  uses-material-design: true

  assets:
    - assets/
